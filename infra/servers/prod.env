# ===== Identity =====
ENV_NAME=prod
SERVER_HOST=167.71.182.198
SERVER_SSH_PORT=22
SERVER_USER=root
SERVER_APP_DIR=/opt/blogwordpress

# Optional host key pinning (ssh-keyscan output fingerprint SHA256)
# SERVER_HOSTKEY_SHA256=SHA256:replace_me

# ===== WordPress App Image (built from separate app repo) =====
IMAGE_REGISTRY=andiaryanto11
IMAGE_NAME=apppowershiftreport
IMAGE_TAG=latest
NGINX_IMAGE=nginx:1.27-alpine

# Optional
DEPLOY_USER=deploy
APP_HTTP_PORT=80
